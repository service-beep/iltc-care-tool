name = "billowing-river-dcf6iltc-backend"
main = "worker.js"
compatibility_date = "2024-09-23"

# D1 與 KV binding 暫時用 Cloudflare Dashboard 手動設定。
# 拿到 ID 後再把下面區塊取消註解、填入真實 UUID，commit 後永久穩定。
#
# [[d1_databases]]
# binding = "DB"
# database_name = "iltc-data"
# database_id = "REPLACE_WITH_REAL_UUID"
#
# [[kv_namespaces]]
# binding = "KV"
# id = "REPLACE_WITH_REAL_KV_ID"

[vars]
# 公開變數可寫在這（目前沒有）
